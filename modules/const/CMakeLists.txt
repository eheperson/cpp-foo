#
#
# We will put the library into a subdirectory called 'constKeyword'
# We have to add thatlne to the 'CMakeLists.txt' in the 'ConstKeyword' directory
# add_library(const main.cpp const.h)
add_executable(const main.cpp const.h)



